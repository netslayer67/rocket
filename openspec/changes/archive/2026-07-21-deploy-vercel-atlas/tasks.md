## 1. Serverless API

- [x] 1.1 Extract shared Nest application configuration and add a cached Vercel API handler.
- [x] 1.2 Add Vercel configuration for the API project without changing existing API routes.
- [x] 1.3 Verify API compilation and focused handler configuration tests.

## 2. Atlas migration

- [x] 2.1 Add a one-time local migration command that copies only Rocket collections and reports counts.
- [x] 2.2 Run the migration from local MongoDB to Atlas and verify matching counts.

## 3. Online deployment

- [x] 3.1 Push deployment code without environment files or Markdown documentation.
- [x] 3.2 Create/link Vercel API and web projects, configure production environment variables, and deploy both.
- [x] 3.3 Configure deployed CORS and public API URL, then smoke-test online endpoints.
- [x] 3.4 Run line-limit checks, tests, and workspace build.
