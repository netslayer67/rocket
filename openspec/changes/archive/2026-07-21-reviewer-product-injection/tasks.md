## 1. Reviewer DNA

- [x] 1.1 Add Product Injection Score, marketplace-language, reasoning-flow, and known topic-drift checks to the existing reviewer.
- [x] 1.2 Extend generation and rewrite instructions with the new constraints.
- [x] 1.3 Add regression tests for the beach-wedding/batik failure and a coherent reference bridge.

## 2. Verification and handoff

- [x] 2.1 Run `npm run check:lines`, `npm test`, and `npm run build`.
- [x] 2.2 Sync specs into `openspec/specs/` and archive the change.
