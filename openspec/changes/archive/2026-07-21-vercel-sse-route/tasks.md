## 1. Route compatibility

- [x] 1.1 Change the API SSE route to read a validated `jobId` query parameter.
- [x] 1.2 Update the browser EventSource URL and tests/spec references.

## 2. Verification

- [x] 2.1 Run line checks, API tests, and builds.
- [x] 2.2 Redeploy API and web, verify production `jobId` plus SSE events, sync specs, and archive the change.
