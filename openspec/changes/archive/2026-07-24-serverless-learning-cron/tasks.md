## 1. Cron endpoint

- [x] 1.1 Add the authenticated GET cron endpoint and reuse the existing bounded learning service.
- [x] 1.2 Add Vercel cron configuration and document `CRON_SECRET` in `.env.example`.
- [x] 1.3 Add unit coverage for valid, invalid, and empty cron requests.

## 2. Verification

- [x] 2.1 Run line, test, build, and strict OpenSpec validation checks.
- [x] 2.2 Sync the delta specs and archive the completed change.
