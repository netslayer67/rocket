## 1. Graph implementation

- [x] 1.1 Replace the rigid monitoring graph with typed stage/node/path data and curved branched SVG wires.
- [x] 1.2 Bind node, path, particle, and status states to the existing recent persisted event filter; keep idle history honest.
- [x] 1.3 Add semantic labels, legend text, reduced-motion handling, and narrow-viewport overflow behavior.

## 2. Context and verification

- [x] 2.1 Record the anti-slop, contrast, keyboard, responsive, overflow, and reduced-motion review in context/AI-SLOP.md.
- [x] 2.2 Run `npm run check:lines`, `npm test`, `npm run build`, and `openspec validate --all --strict`.
