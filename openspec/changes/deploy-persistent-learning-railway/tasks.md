## 1. Railway-ready API

- [x] 1.1 Add and test a minimal API liveness endpoint for Railway health checks.
- [x] 1.2 Add root Railway config that builds and runs only the API workspace as a persistent service.

## 2. Deployment handoff

- [x] 2.1 Document Railway variables, single-replica continuous-learning settings, cutover, OAuth update, and rollback steps without committing secrets.
- [x] 2.2 Verify API tests, line limits, and production builds.

## 3. Authenticated deployment

- [ ] 3.1 Connect the Railway account, deploy the configured service, set secrets, and verify the Railway domain before switching the Vercel web API URL.
